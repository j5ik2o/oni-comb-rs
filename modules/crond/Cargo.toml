[package]
name = "oni-comb-crond"
version = "3.0.0"
edition = "2021"

[dependencies]
oni-comb-parser = { path = "../parser" }
chrono = { version = "0.4.20", default-features = false, features = ["std"] }

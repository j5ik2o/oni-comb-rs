[package]
name = "oni-comb-crond"
version = "0.1.0"
edition = "2021"

[dependencies]
oni-comb-parser = { path = "../parser" }
chrono = { version = "0.4", default-features = false, features = ["std"] }

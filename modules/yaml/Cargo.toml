[package]
name = "oni-comb-yaml"
version = "3.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "YAML 1.2 parser built on oni-comb-parser"

[dependencies]
oni-comb-parser = { path = "../parser" }

[dev-dependencies]
trybuild = "1"

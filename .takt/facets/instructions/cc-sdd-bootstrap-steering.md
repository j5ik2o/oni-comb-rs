コードベースを分析し、`.kiro/steering/` に初期steeringファイルを生成せよ。

**やること:**
1. `.takt/knowledge/cc-sdd-steering-template-files/` からテンプレートを読み込む
2. コードベースが存在するか確認する（ソースファイル、README、package.json、Cargo.toml等）
3. コードベースが存在する場合はJIT（必要時に取得）で分析する:
   - ソースファイルの構成を調査
   - README、package.json、Cargo.toml等のプロジェクト定義ファイルを読む
   - コードパターンを検索する
4. パターンを抽出する（リストではなく）:
   - **product.md**: プロダクトの目的、価値、コア機能
   - **tech.md**: フレームワーク、技術的決定、規約
   - **structure.md**: コード構成、命名規則、インポートパターン
5. テンプレートに従ってsteeringファイルを生成する
6. `.kiro/steering/` ディレクトリに保存する（存在しなければ作成）

**コードベースが存在しない場合（グリーンフィールド）:**
- テンプレートをベースにひな型を生成する
- プレースホルダ（`[選択肢]`、`[理由]`、`[技術スタック]` 等）は残し、開発者が記入できるようにする
- ユーザーのタスク入力にプロダクトの方針・技術選定があればそれを反映する
- コードがないことを理由にABORTしてはならない

**JIT戦略:** 情報は必要になった時点で取得する。事前に全ファイルを読まない。

**フォーカス:** 意思決定を導くパターンを記録する。ファイルやモジュールのカタログは作らない。

**成果物の保存先:**
- ディレクトリ: `.kiro/steering/`（存在しなければ作成）
- ファイル: `product.md`, `tech.md`, `structure.md`

```markdown
# レガシーコード分析レポート

## サマリー
{検出件数と削除可能件数の要約}

## 検出結果

### 高優先度（即削除可能：呼び出し元ゼロ）

| # | ファイル | 行 | 種別 | 対象 | 根拠 |
|---|---------|-----|------|------|------|
| 1 | `src/file.rs:42` | 42 | dead_code | `fn old_method()` | grep結果: 0件 |

### 中優先度（移行後に削除可能）

| # | ファイル | 行 | 種別 | 対象 | 呼び出し元 | 移行方針 |
|---|---------|-----|------|------|-----------|---------|
| 1 | `src/file.rs:100` | 100 | deprecated | `fn legacy_parse()` | `tests/old.rs:5` | 新APIに書き換え |

### 低優先度（段階的対応が必要）

| # | ファイル | 行 | 種別 | 対象 | 理由 |
|---|---------|-----|------|------|------|
| 1 | `src/file.rs:200` | 200 | pub API | `pub fn compat_v1()` | crates.io公開API、SemVer対応が必要 |

## 削除計画

### ステップ1: {説明}
- 対象: {ファイルリスト}
- 確認: `cargo build --workspace`

### ステップ2: {説明}
- 対象: {ファイルリスト}
- 確認: `cargo test --workspace`

## 削除対象外（今回スコープ外）
- {理由付きリスト}
```
